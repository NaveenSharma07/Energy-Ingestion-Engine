{
  "info": {
    "name": "Energy Ingestion Engine",
    "description": "Postman collection for the Energy Ingestion Engine API",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Health Check",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/health",
          "host": ["{{baseUrl}}"],
          "path": ["health"]
        },
        "description": "Checks service + DB health. Test accepts either `{ status: 'healthy' }` or the current simple `{ status: 200, message: 'Server is running' }` response."
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "pm.test('Service responds 200', function () {",
              "  pm.response.to.have.status(200);",
              "});",
              "",
              "pm.test('Service is healthy or compatible legacy response', function () {",
              "  var json;",
              "  try { json = pm.response.json(); } catch (e) { pm.fail('Response is not valid JSON'); }",
              "  var ok = (json.status === 'healthy' || json.status === 200 || json.message === 'Server is running');",
              "  pm.expect(ok).to.be.true;",
              "});"
            ]
          }
        }
      ]
    },
    {
      "name": "Ingest Meter Telemetry",
      "request": {
        "method": "POST",
        "header": [{ "key": "Content-Type", "value": "application/json" }],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"meterId\": \"{{meterId}}\",\n  \"kwhConsumedAc\": 125.5,\n  \"voltage\": 240.0,\n  \"timestamp\": \"{{timestamp}}\"\n}",
          "options": { "raw": { "language": "json" } }
        },
        "url": {
          "raw": "{{baseUrl}}/v1/ingestion/meter",
          "host": ["{{baseUrl}}"],
          "path": ["v1", "ingestion", "meter"]
        },
        "description": "POST meter telemetry. Uses environment variable `meterId` and `timestamp` (set below)."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "// Set a dynamic ISO timestamp before the request",
              "pm.environment.set('timestamp', new Date().toISOString());"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "pm.test('Meter ingested (201 & success=true)', function () {",
              "  pm.response.to.have.status(201);",
              "  var json = pm.response.json();",
              "  pm.expect(json.success).to.eql(true);",
              "  pm.expect(json.meterId).to.eql(pm.environment.get('meterId'));",
              "});"
            ]
          }
        }
      ]
    },
    {
      "name": "Ingest Vehicle Telemetry",
      "request": {
        "method": "POST",
        "header": [{ "key": "Content-Type", "value": "application/json" }],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"vehicleId\": \"{{vehicleId}}\",\n  \"soc\": 85.5,\n  \"kwhDeliveredDc\": 105.2,\n  \"batteryTemp\": 28.5,\n  \"timestamp\": \"{{timestamp}}\"\n}",
          "options": { "raw": { "language": "json" } }
        },
        "url": {
          "raw": "{{baseUrl}}/v1/ingestion/vehicle",
          "host": ["{{baseUrl}}"],
          "path": ["v1", "ingestion", "vehicle"]
        },
        "description": "POST vehicle telemetry. Uses environment variable `vehicleId` and dynamic `timestamp`."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "// Set a dynamic ISO timestamp before the request",
              "pm.environment.set('timestamp', new Date().toISOString());"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "pm.test('Vehicle ingested (201 & success=true)', function () {",
              "  pm.response.to.have.status(201);",
              "  var json = pm.response.json();",
              "  pm.expect(json.success).to.eql(true);",
              "  pm.expect(json.vehicleId).to.eql(pm.environment.get('vehicleId'));",
              "});"
            ]
          }
        }
      ]
    },
    {
      "name": "Get Vehicle Performance (24h)",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/v1/analytics/performance/{{vehicleId}}",
          "host": ["{{baseUrl}}"],
          "path": ["v1", "analytics", "performance", "{{vehicleId}}"]
        },
        "description": "Fetches 24-hour performance for `vehicleId`. If a meter mapping is not present, AC total will be 0."
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "pm.test('Analytics returned 200 and contains expected fields', function () {",
              "  pm.response.to.have.status(200);",
              "  var json = pm.response.json();",
              "  pm.expect(json.vehicleId).to.eql(pm.environment.get('vehicleId'));",
              "  pm.expect(json).to.have.property('totalEnergyDeliveredDc');",
              "  pm.expect(json).to.have.property('efficiencyRatio');",
              "});"
            ]
          }
        }
      ]
    },
    {
      "name": "Manual: Add meter-vehicle mapping (SQL)",
      "request": {
        "method": "POST",
        "header": [],
        "body": {
          "mode": "raw",
          "raw": "-- This is a manual SQL snippet to run in the DB if you want analytics to include AC consumption:\n-- INSERT INTO meter_vehicle_mapping (meter_id, vehicle_id) VALUES ('meter-001', 'vehicle-001');",
          "options": { "raw": { "language": "sql" } }
        },
        "url": {
          "raw": "-- RUN IN DB (psql / pgAdmin)",
          "host": ["-- RUN IN DB (psql / pgAdmin)"]
        },
        "description": "This request is a non-executable helper: run the SQL in your Postgres instance to map a meter to a vehicle so analytics can compute AC consumption."
      }
    }
  ]
}
